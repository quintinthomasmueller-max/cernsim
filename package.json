{
  "name": "cernsim-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CERN-Stellwerk Web-App — Build (esbuild) & Headless-Tests (Vitest/jsdom). Siehe docs/MIGRATION.md.",
  "scripts": {
    "build": "node cern/app/esbuild.mjs",
    "pretest": "python3 scripts/sync_widget.py",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
